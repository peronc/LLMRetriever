# setup.py
from setuptools import setup, find_packages

setup(
    name="LLMRetriever",
    version="0.1",
    packages=find_packages(),
    install_requires=[],  # Add dependencies if neededs
    author="Peron Carlo",
    author_email="peronc79@gmail.com",
    description="A small Python library useful to perform a retriever by LLM",
    url="https://github.com/peronc/LLMRetriever",  # Optional: link to your GitHub
)

